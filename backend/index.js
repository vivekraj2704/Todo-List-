const express = require('express');
const app = express(); 

app.use(express.json());

app.post('/todo', () => {
      
})

app.get('/todos', () => {
      
})

app.put('/completed', () => {
      
})

app.listen(3000)